---
phase: 02.1-editor-edit-mode-diff-view-tiptap-migration-edit-save-search-inline-diff-markers-for-rejection-feedback
plan: 02
subsystem: ui
tags: [tiptap, prosemirror, markdown, editor, zustand, react]

# Dependency graph
requires:
  - phase: 02.1-01
    provides: TipTap 3.20.5 dependencies installed, EditorTab type with mode/isDirty, POST /api/files/save endpoint
provides:
  - TipTapEditor component rendering markdown via TipTap 3 with all extensions
  - EditorToolbar with read/edit mode toggle (Lock/Pencil icons)
  - Extended Zustand store with setTabMode, setTabDirty, snapshot capture/get/clear
  - Rewritten DocsView composing TabBar + EditorToolbar + TipTapEditor
  - Dirty tab indicator in TabBar
  - WebSocket guard preventing live-reload from overwriting dirty edits
affects: [02.1-03-save-search, 02.1-04-diff-overlay, 02.1-05-integration]

# Tech tracking
tech-stack:
  added: []
  patterns: [TipTap useEditor with contentType markdown, editor.setEditable for runtime mode toggle, scroll preservation via scrollTop capture/restore around setContent, snapshot Map in Zustand for diff comparison]

key-files:
  created:
    - packages/frontend/src/components/docs/TipTapEditor.tsx
    - packages/frontend/src/components/docs/EditorToolbar.tsx
  modified:
    - packages/frontend/src/store/index.ts
    - packages/frontend/src/views/DocsView.tsx
    - packages/frontend/src/components/docs/TabBar.tsx
    - packages/frontend/src/hooks/useWebSocket.ts

key-decisions:
  - "User edits update content and isDirty atomically via useAppStore.setState (not updateTabContent which resets isDirty for live-reload)"
  - "TipTapEditor uses key={activeTab.id} to create fresh editor per tab (avoids stale ProseMirror state)"

patterns-established:
  - "TipTap editor mode toggle: read-only by default, toggle via editor.setEditable() in useEffect"
  - "Scroll preservation: capture containerRef.scrollTop before setContent, restore via requestAnimationFrame"
  - "Dirty guard in WebSocket: skip updateTabContent when tab.mode === 'edit' && tab.isDirty"

requirements-completed: [EDIT-05, EDIT-07]

# Metrics
duration: 4min
completed: 2026-03-25
---

# Phase 02.1 Plan 02: TipTap Migration + Edit Mode Toggle Summary

**TipTap 3 editor replacing MarkdownViewer with read/edit mode toggle, scroll preservation, dirty tracking, and snapshot support for diff view**

## Performance

- **Duration:** 4 min
- **Started:** 2026-03-25T08:20:06Z
- **Completed:** 2026-03-25T08:24:42Z
- **Tasks:** 2
- **Files modified:** 7 (1 deleted, 2 created, 4 modified)

## Accomplishments
- TipTapEditor renders markdown with full fidelity (headings, lists, code blocks, task checkboxes) via StarterKit + Markdown + TaskList + TaskItem + CodeBlockLowlight + Placeholder
- EditorToolbar provides visual mode toggle with Lock (read-only) and Pencil (editing) icons
- Zustand store extended with mode/dirty tracking per tab and snapshot capture/get/clear for future diff overlay
- DocsView rewritten to compose TabBar + EditorToolbar + TipTapEditor, MarkdownViewer deleted
- WebSocket handler guards dirty tabs from being overwritten by live-reload events

## Task Commits

Each task was committed atomically:

1. **Task 1: Create TipTapEditor component and extend Zustand store** - `5e41072` (feat)
2. **Task 2: Create EditorToolbar and rewrite DocsView** - `939b21d` (feat)

## Files Created/Modified
- `packages/frontend/src/components/docs/TipTapEditor.tsx` - TipTap 3 editor with all extensions, scroll preservation, editable toggle
- `packages/frontend/src/components/docs/EditorToolbar.tsx` - Mode toggle toolbar with Lock/Pencil icons and dirty indicator
- `packages/frontend/src/store/index.ts` - Extended with setTabMode, setTabDirty, snapshotTabs, captureSnapshot, getSnapshot, clearSnapshot
- `packages/frontend/src/views/DocsView.tsx` - Rewritten to compose TipTapEditor + EditorToolbar (replaces MarkdownViewer)
- `packages/frontend/src/components/docs/TabBar.tsx` - Added isDirty prop and dirty dot indicator
- `packages/frontend/src/hooks/useWebSocket.ts` - Added guard to skip live-reload for dirty tabs in edit mode
- `packages/frontend/src/components/docs/MarkdownViewer.tsx` - Deleted (replaced by TipTapEditor)

## Decisions Made
- User edits update content and isDirty atomically via useAppStore.setState rather than using updateTabContent (which resets isDirty for live-reload use case)
- TipTapEditor uses key={activeTab.id} to create a fresh ProseMirror instance per tab, avoiding stale state when switching between documents

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Fixed isDirty being reset on user edits**
- **Found during:** Task 2 (DocsView rewrite)
- **Issue:** handleUpdate called setTabDirty(true) then updateTabContent which sets isDirty=false, resulting in dirty flag never being true
- **Fix:** handleUpdate now uses useAppStore.setState to atomically set content and isDirty=true instead of going through updateTabContent
- **Files modified:** packages/frontend/src/views/DocsView.tsx
- **Verification:** TypeScript compiles, isDirty correctly set to true on user edit
- **Committed in:** 939b21d (Task 2 commit)

---

**Total deviations:** 1 auto-fixed (1 bug fix)
**Impact on plan:** Essential for correct dirty tracking. No scope creep.

## Issues Encountered
None

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- TipTap editor and mode toggle are in place, ready for save/search (Plan 03)
- Snapshot infrastructure ready for diff overlay (Plan 04)
- WebSocket dirty guard prevents data loss during edit mode

## Self-Check: PASSED

All created files verified present. All commits verified in git log. MarkdownViewer confirmed deleted.

---
*Phase: 02.1-editor-edit-mode-diff-view-tiptap-migration-edit-save-search-inline-diff-markers-for-rejection-feedback*
*Completed: 2026-03-25*
